package jenkins;

public class initialize_jenkins {

	/*
	 * To initialize jenkins from the command line:
	 * -navigate to directory with jenkins.war file
	 * enter:
	 * 		java -jar jenkins.war --httpPort=8888
	 */
}
